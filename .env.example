# Database Connection
# Defaults to SQLite if omitted. In production on Render/Railway/AWS, use PostgreSQL.
DATABASE_URL=sqlite:///./smartaid.db

# JWT Secret Key for Session & Cryptographic Token Generation
SMARTAID_SECRET_KEY=change-this-to-a-secure-random-64-character-hex-string-for-production

# Server Port
PORT=8000
